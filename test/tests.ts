import { describe, it } from 'mocha';
import * as chai from 'chai';

describe('just a test', () => {

    it('should test the mocha', () => {
        chai.expect('foo').to.be.equal('foo');
    })

})
