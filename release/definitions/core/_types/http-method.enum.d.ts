/**
 * Choose the HTTP Method
 *
 * There aren't all HTTP methods here,
 * just to simplify.
 */
export declare enum HttpMethod {
    GET = 0,
    HEAD = 1,
    OPTIONS = 2,
    POST = 3,
    PATCH = 4,
    PUT = 5,
    DELETE = 6,
    ALL = 7,
}
